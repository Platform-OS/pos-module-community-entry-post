{% comment %}
  Loads a post's data.

  Params:
    - id: string (optional)
{% endcomment %}
{% liquid
  graphql posts = 'modules/community-entry-post/post/search', id: id, page: 1, per_page: 1
  if posts.errors
    return posts
  endif

  return posts.posts.results.first
%}
